<div class="content">
  <form>
    {{#each sections}}
    <div class="form-group">
      <div class="form-fields">
        <input type="text" name="flags.zhell-custom-stuff.inventorySections" value="{{this}}">
      </div>
    </div>
    {{/each}}
    <div class="form-group">
      <div class="form-fields">
        <input type="text" name="flags.zhell-custom-stuff.inventorySections">
      </div>
    </div>
    <footer>
      <button type="submit">
        <i class="fa-solid fa-backpack"></i> {{localize "Save"}}
      </button>
    </footer>
  </form>
</div>
